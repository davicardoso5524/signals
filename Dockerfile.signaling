FROM node:22-alpine

WORKDIR /app
RUN npm init -y >/dev/null 2>&1 && npm install --omit=dev @supabase/supabase-js ws tsx
COPY server ./server

ENV NODE_ENV=production
EXPOSE 8787
CMD ["node_modules/.bin/tsx", "server/signaling.ts"]
