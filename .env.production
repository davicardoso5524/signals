# Public signaling endpoint embedded in production web and Tauri builds.
VITE_SIGNALING_URL=wss://signals-signaling.onrender.com
# Production uses direct ICE first; TURN is supplied by the signaling service when available.
VITE_WEBRTC_FORCE_RELAY=false
